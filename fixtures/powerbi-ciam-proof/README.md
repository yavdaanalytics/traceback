# Power BI CIAM proof fixture

Redacted snapshot of the Jul 8, 2026 real-world measurement documented in [`actual_codebase_measures/powerbi-embedded-analytics-ciam-search.md`](../../actual_codebase_measures/powerbi-embedded-analytics-ciam-search.md).

**`invocation-1.json`** — telemetry fields only (query, mode, line counts, timestamps). No machine paths, no live SQLite DB.

The measured repo is a **private** production codebase (~100K LOC, Azure CIAM auth). You cannot clone it from GitHub; reproduce on your own repo with `npm run proof:powerbi` or document a new row under `actual_codebase_measures/`.
